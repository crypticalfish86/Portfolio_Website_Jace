export const SingleProjectFolder = (props) =>
{
    const images = props.images;//all images of the work object
    const txtDescription = props.txtDescription; //the .txt file path for the work object
    console.log(txtDescription)

    return(
        <div id="Single_Project_Folder">
            SingleProjectFolder
        </div>
    )
}